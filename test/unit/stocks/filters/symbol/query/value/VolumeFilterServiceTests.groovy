package stocks.filters.symbol.query.value


import grails.test.mixin.*
import stocks.filters.symbol.value.VolumeFilterService

/**
 * See the API for {@link grails.test.mixin.services.ServiceUnitTestMixin} for usage instructions
 */
@TestFor(VolumeFilterService)
class VolumeFilterServiceTests {

    void testSomething() {
        fail "Implement me"
    }
}
